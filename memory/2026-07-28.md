# 2026-07-28 — Daily log

## Tóm tắt
Sếp yêu cầu review lại kế hoạch dự án. Em audit toàn bộ tài liệu + code, tìm
ra 15 điểm không ổn giữa tài liệu và codebase. Sếp chỉ đạo:

1. Merge `audit.md` + `master-plan.md` vào `tasks.md` → xoá 2 file cũ.
2. Cập nhật tài liệu theo 15 điểm đã liệt kê.
3. Tạo file `.md` cho tasks 07-14 (chỉ có 00-06 trước đó).
4. Viết `docs/design.md` — tài liệu thiết kế dùng chung.
5. Bắt đầu ghi `MEMORY.md` + daily memory.

## Việc đã làm

- Đọc toàn bộ: SOUL.md, USER.md, IDENTITY.md, TOOLS.md, AGENTS.md,
  CLAUDE.md, master-plan.md, tasks.md, audit.md, README.md, CHANGELOG.md.
- `git log --oneline` cho thấy 65 commit, ~30 commit gần nhất toàn fix UI.
- `go build ./...` PASS, `go test ./...` core PASS (các package khác
  "no test files").
- Phát hiện 15 vấn đề (xem MEMORY.md "Tồn đọng còn lại").
- Tạo `docs/tasks/07-multi-user.md` → `14-split-webui.md` (8 file).
- Sync `docs/database/schema.sql` từ `store.go` migrations.
- Viết `docs/design.md` (183 dòng) — kiến trúc + quy ước code.
- Viết `MEMORY.md` và `memory/2026-07-28.md` (file này).

## Chưa làm trong hôm nay

- Merge master-plan + audit vào tasks.md, xóa file gốc.
- Cập nhật README.md + CHANGELOG.md cho khớp thực tế.
- Cập nhật CLAUDE.md tham chiếu `docs/design.md`.

## Ghi chú kỹ thuật hôm nay

- Schema trong `store.go` có 26 cột `media`, schema.sql chỉ có 12 → đã sync.
- `oa_configs` migration có `oa_name` mà schema.sql không có → đã thêm.
- `oa_webhook_logs` migration có `event_id`, `sender_id`, `error_msg` và
  index `idx_oa_pending` → đã sync.
- Binary `zcloudd` (16MB) ở cả `/data/zcloud/` và `src/zcloud/`.
- `.gitignore` đã ignore `zcloudd` nhưng binary cũ vẫn còn trong history.

## Commit hôm nay
(sẽ commit sau khi tất cả file xong)
